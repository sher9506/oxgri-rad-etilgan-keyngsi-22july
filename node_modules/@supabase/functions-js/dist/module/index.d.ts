export declare class FunctionsClient {
  protected url: string;
  protected headers: Record<string, string>;
  protected region?: string;

  constructor(url: string, options?: {
    headers?: Record<string, string>;
    customFetch?: typeof fetch;
    region?: string;
  });

  setAuth(token: string): void;

  invoke<T = any>(
    functionName: string,
    options?: {
      headers?: Record<string, string>;
      method?: string;
      body?: any;
      region?: string;
    }
  ): Promise<{ data: T; error: null } | { data: null; error: FunctionsHttpError | FunctionsRelayError | FunctionsFetchError }>;
}

export declare class FunctionsHttpError extends Error {
  context: Response;
  constructor(context: Response);
}

export declare class FunctionsRelayError extends Error {
  context: Response;
  constructor(context: Response);
}

export declare class FunctionsFetchError extends Error {
  context: Error;
  constructor(context: Error);
}
