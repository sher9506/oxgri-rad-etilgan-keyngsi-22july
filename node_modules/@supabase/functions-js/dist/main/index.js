// @supabase/functions-js shim v2.115.0 - CJS
'use strict';
Object.defineProperty(exports, '__esModule', { value: true });

class FunctionsError extends Error {
  constructor(message, name, context) {
    super(message);
    this.name = name || 'FunctionsError';
    this.context = context;
  }
}
exports.FunctionsError = FunctionsError;

class FunctionsHttpError extends FunctionsError {
  constructor(context) {
    super('Edge Function returned a non-2xx status code', 'FunctionsHttpError', context);
  }
}
exports.FunctionsHttpError = FunctionsHttpError;

class FunctionsRelayError extends FunctionsError {
  constructor(context) {
    super('Relay error invoking edge function', 'FunctionsRelayError', context);
  }
}
exports.FunctionsRelayError = FunctionsRelayError;

class FunctionsFetchError extends FunctionsError {
  constructor(context) {
    super('Failed to send a request to the Edge Function', 'FunctionsFetchError', context);
  }
}
exports.FunctionsFetchError = FunctionsFetchError;

class FunctionsClient {
  constructor(url, options) {
    this.url = url;
    this.headers = (options && options.headers) || {};
  }
  setAuth(token) { this.headers['Authorization'] = 'Bearer ' + token; }
  async invoke(functionName, options) {
    const opts = options || {};
    const url = this.url + '/' + functionName;
    const headers = Object.assign({}, this.headers, opts.headers || {});
    if (opts.body && typeof opts.body === 'object' && !(opts.body instanceof FormData) && !(opts.body instanceof Blob)) {
      headers['Content-Type'] = 'application/json';
    }
    try {
      const res = await fetch(url, {
        method: opts.method || 'POST',
        headers,
        body: opts.body
          ? (typeof opts.body === 'string' ? opts.body : opts.body instanceof FormData || opts.body instanceof Blob ? opts.body : JSON.stringify(opts.body))
          : undefined,
      });
      let data = null;
      let error = null;
      if (!res.ok) {
        error = new FunctionsHttpError(res);
      } else {
        const contentType = res.headers.get('Content-Type') || '';
        if (contentType.includes('application/json')) {
          data = await res.json();
        } else {
          data = await res.text();
        }
      }
      return { data, error };
    } catch (e) {
      return { data: null, error: new FunctionsFetchError(e) };
    }
  }
}
exports.FunctionsClient = FunctionsClient;
